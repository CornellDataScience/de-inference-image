apiVersion: apps/v1
kind: Deployment
metadata:
  name: de-inference-deployment
  labels:
    app: de-inference
spec:
  replicas: 5
  selector:
    matchLabels:
      app: de-inference
  template:
    metadata:
      labels:
        app: de-inference
    spec:
      containers:
      - name: de-inference
        image: de-inference:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
